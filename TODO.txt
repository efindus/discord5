P1:
restore scrollheight on reconnects (using the new parameters for the /api/messages endpoint)
pasteable attachments
remove "innerHTML = sth" wherever possible (to improve performance)
add admin panel with user bans and username allowlist

P2:
consider a scroll-to-bottom button
improve online bar
ipbans after consistently hitting the rate limit
ipbans after some treshold of points over the limit
hostname resolution

P3:
fix mobile
add replies
add mentions
add avatars
